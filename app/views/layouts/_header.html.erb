<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">
  Funny Movies</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
    </ul>
    <% if user_signed_in? %>
      <span>Welcome <%= current_user.email %></span>
      <button class="btn btn-outline-success my-2 my-sm-0" style="margin: 10px 10px;">Share a movie</button>
      <%= link_to('Logout', destroy_user_session_path, method: :delete, class: "btn btn-outline-success my-2 my-sm-0") %>
    <% else %>
      <%= form_for(:user, :url => session_path(:user), class: "form-inline my-2 my-lg-0", method: :post) do |f| %>
        <div class="row">
          <div class="col">
            <%= f.text_field :email, class: "form-control", placeholder: "Email", autocomplete: "off" %>
          </div>
          <div class="col">
            <%= f.password_field :password, class: "form-control", placeholder: "Password", autocomplete: "off" %>
          </div>
          <div class="col">
            <%= f.submit 'Login / Register', class: "btn btn-outline-success my-2 my-sm-0" %>
          </div>
        </div>
      <% end %>
    <% end %>
  </div>
</nav>
